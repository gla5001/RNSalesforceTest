# Salesforce React Native test app
iOS only
- Install deps

    `npm install`

- To Run, need to install react-native `npm install -g react-native`
    
    `npm start` in terminal
    
- Open second terminal

    `react-native run-ios`
 